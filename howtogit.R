# read this
#
# need to edit and the push directly to the .io using examples here:
# https://www.caitlincasar.com/post/blogdown/


#To the architecture (do this when making changes):

git remote add origin https://github.com/chrissuthy/webpage-sutherland
git add .
git commit -a -m "updated"
git push -u origin master


#To the website (do this to push changes to github pages / io):

git remote add origin https://github.com/chrissuthy/chrissuthy.github.io
git add .
git commit -a -m "updated"
git push -u origin master
